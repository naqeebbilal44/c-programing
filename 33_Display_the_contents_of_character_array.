//Display contents of a character array
#include <stdio.h>

int main() {
    char arr[] = {'H', 'e', 'l', 'l', 'o'};
    int i;

    printf("Character array:\n");
    for (i = 0; i < 5; i++) {
        printf("%c ", arr[i]);
    }

    return 0;
}