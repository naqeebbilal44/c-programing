//. Display the series: 2 4 8 16 32 64 128 ....... (n terms)

#include <stdio.h>

int main() {
    int i, n, x = 2;

    printf("Enter n\n");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
        printf("%d ", x);
        x = x * 2;
    }

    return 0;
}
